length = float(input(" enter the length of the rectiangle"))
width = float(input("enter the width of the rectiangle"))

area = length * width
perimeter = 2 * (length + width)

print(f"area of the rectangle:  {area}")
print(f" perimeter of the rectangle:  {perimeter}")
